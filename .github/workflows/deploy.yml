name: Deploy to GitHub Pages

on:
  push:
    branches: "main"

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build_site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

  deploy:
    needs: build_site
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
```

4. Click **"Commit changes..."** at the top right to save it.

---

### 2. CRITICAL: Update your Repo Name
I noticed in your screenshot that your repository name is `AI-ML_Learning_Program_Tracker_App`. For the links to work, your `vite.config.js` file **must** match this name exactly.

Go to your `vite.config.js` file in GitHub and make sure the `base` line looks like this:

```javascript
// Change the base to match your exact repo name
export default defineConfig({
  plugins: [react()],
  base: '/AI-ML_Learning_Program_Tracker_App/', 
})
